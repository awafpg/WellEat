import { render } from "@testing-library/react";
import { describe, expect, it } from "vitest";

describe("Footer", () => {
  // 1. check apakah footer ter render
  it("should render Footer component", () => {
    const renderedFooter = render(<footer />);

    expect 
  });
});
